
<section class="grid grid-cols-3 gap-5 mx-auto mt-12 lg:max-w-none">
  {% set postslist = collections.post %}
  {% for post in postslist | reverse %}

  <div class="flex flex-col overflow-hidden rounded-lg shadow-lg">
    <div class="flex-shrink-0">
      <img class="object-cover w-full h-48" src="{{ post.data.image }}" alt="">
    </div>
    <div class="flex flex-col justify-between flex-1 p-6 bg-white">
      <div class="flex-1">
        <p class="text-sm font-medium text-indigo-600">
          <a href="#" class="hover:underline">
            Article
          </a>
        </p>
        <a href="{{ post.url | url }}" class="block mt-2">
          <p class="text-xl font-semibold text-gray-900 hover:underline">
            {% if post.data.title %}
                {{ post.data.title }}
            {% else %}
                Untitled
            {% endif %}
          </p>
          <p class="mt-3 text-base text-gray-500">
            {{ post.data.description }}
          </p>
        </a>
      </div>
    </div>
  </div>


    <!-- <article class="flex my-8 space-x-6">
      <div class="w-32 h-32 bg-center bg-no-repeat rounded shadow-xl bg-blueGray-400 sm:bg-contain lg:bg-cover" style="background-image: url({{ post.data.image }})"></div>
      <div class="flex-1">
        <h2 class="mb-2 text-2xl font-bold text-blueGray-200 hover:text-blueGray-200 hover:underline">
            <a href="{{ post.url | url }}">
                {% if post.data.title %}
                    {{ post.data.title }}
                {% else %}
                    Untitled
                {% endif %}
            </a>
        </h2>
        <span class="inline-flex mb-2 items-center px-3 py-0.5 rounded-full text-sm font-medium bg-blue-100 text-blue-800">
          <time>{{ post.date | readableDate }}</time>
        </span>
        {% if post.data.description %}
            <p class="mb-3 text-base text-blueGray-300">
                {{ post.data.description }}
            </p>
        {% endif %}
        <p>
            <a href="{{ post.url | url }}" class="text-lg font-bold text-pink-400">
                Read more →
            </a>
        </p>
      </div>
    </article> -->
  {% endfor %}
</section>
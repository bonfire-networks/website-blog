{# 
<section class="grid grid-cols-3 gap-5 mx-auto mt-12 lg:max-w-none">
  {% set postslist = collections.post %}
  {% for post in postslist | reverse %}
    {% if loop.index0 < 3 %}
      <div class="flex flex-col col-span-3 overflow-hidden rounded-lg shadow-lg lg:col-span-1">
        <div class="flex-shrink-0">
          <img class="object-cover w-full h-48" src="{{ post.data.image }}" alt="">
        </div>
        <div class="flex flex-col justify-between flex-1 p-6 bg-white">
          <div class="flex-1">
            <p class="text-sm font-medium text-indigo-600">
              <a href="#" class="hover:underline">
                Article
              </a>
            </p>
            <a href="{{ post.url | url }}" class="block mt-2">
              <p class="text-xl font-semibold text-gray-900 hover:underline">
                {% if post.data.title %}
                    {{ post.data.title }}
                {% else %}
                    Untitled
                {% endif %}
              </p>
              <p class="mt-3 text-base text-gray-500">
                {{ post.data.description }}
              </p>
            </a>
          </div>
        </div>
      </div>
    {% endif %}
  {% endfor %}
</section> #}

<section class="mt-6 lg:px-0">
  {% set postslist = collections.post %}
  {% for post in postslist | reverse %}
  {% if loop.index0 < 3 %}
    <article class="my-16 lg:my-8 lg:flex lg:space-x-6">
      <div class="w-full h-40 lg:w-32 lg:h-32 bg-blueGray-400 rounded bg-cover bg-no-repeat bg-center shadow-xl" style="background-image: url({{ post.data.image }})"></div>
      <div class="flex-1 mt-4 lg:mt-0">
        <h2 class="font-bold text-2xl text-blueGray-200 hover:text-blueGray-200 hover:underline mb-2">
            <a href="{{ post.url | url }}">
                {% if post.data.title %}
                    {{ post.data.title }}
                {% else %}
                    Untitled
                {% endif %}
            </a>
        </h2>
        <div class="flex items-center space-x-2">
            <span class="flex items-center space-x-1">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-base-content text-opacity-70" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd" />
                  </svg>
              <time class="text-xs text-base-content text-opacity-70">{{ post.date | readableDate }}</time>
            </span>
        </div>
        
        
        {% if post.data.description %}
            <p class="text-sm text-base-content text-opacity-80 my-2">
                {{ post.data.description }}
            </p>
        {% endif %}
        <div class="my-2">
            {%- for tag in post.data.tags -%}
            {%- set tagUrl %}/tags/{{ tag | slug }}/{% endset -%}
                <a href="{{ tagUrl | url }}">
                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-indigo-100 text-indigo-800">
                <svg class="-ml-0.5 mr-1.5 h-2 w-2 text-indigo-400" fill="currentColor" viewBox="0 0 8 8">
                    <circle cx="4" cy="4" r="3" />
                </svg>
                {{ tag }}
                </span>
                </a>
            {%- endfor %}
        </div>
        <div>
            <a href="{{ post.url | url }}" class="text-pink-400 font-bold text-sm">
                Read more →
            </a>
        </div>
      </div>
    </article>
    {% endif %}
  {% endfor %}
</section>
<div class="flex mx-auto mt-12">
  <a href="/blog" class="btn btn-outline btv-sm normal-case">View all posts</a>
</div>
<div class="relative mt-32">
    <span class="relative block text-sm font-semibold tracking-widest uppercase text-base-content">Apps & extensions</span>
    <div class="w-40 h-2 mt-2 transform rounded-full bg-gradient-to-r from-pink-600 to-indigo-600 "></div>
</div>
<div class="mt-4 prose prose-lg">Whether you want to coordinate tasks in your community, organise a mutual aid community or timebank, curate learning resources, grow a community garden or more, there can be the right extension for you!</div>


  <div class="grid grid-cols-1 gap-4 mt-6 lg:grid-cols-3 md:grid-cols-2">
    {% for item in apps %}
      <a href={{ item.link }}>  
        <div class="h-40 p-3 transition-colors transform bg-white shadow-lg cursor-pointer rounded-xl hover:shadow-2xl hover:bg-blueGray-50">
          <div class="flex items-center space-x-3">
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-md text-sm font-medium bg-orange-100 text-orange-800">
                <svg width="14" height="14" class="mr-2" viewBox="0 0 14 14" fill="none"><rect x="1" y="1" width="12" height="12" rx="6" stroke="#f2c94c" stroke-width="2" fill="none"></rect><path fill="#f2c94c" stroke="none" d="M 3.5,3.5 L3.5,0 A3.5,3.5 0 0,1 3.5, 7 z" transform="translate(3.5,3.5)"></path></svg>
                <span>{{item.status}}</span>
            </span>
  
            {% for f in item.flavours %}
              <span class="inline-flex items-center px-2.5 py-0.5 rounded-md text-sm font-medium bg-yellow-100 text-yellow-800">
                  <span>{{f}}</span>
              </span>
            {% endfor %}
          </div>

          <div class="flex items-center mt-2 space-x-3">
            <div style="background-image: url({{item.icon}})" class="w-24 h-24 bg-center bg-no-repeat bg-contain rounded-3xl"></div>
              <div class="flex-1">
              <h2 class="text-base font-bold text-blueGray-700">{{item.name}}</h2>
              <p class="text-sm text-blueGray-700">{{item.description}}</p>
              </div>
          </div>
        </div>  
      </a>
    {% endfor %}
    
  </div>
  
{% include "logos.njk" %}

<div class="p-4 mt-3 rounded-md bg-gradient-to-r from-pink-600 to-indigo-600 ">
<div class="mt-8 text-3xl font-extrabold text-center text-white">Do you need a specific app or extension for your community?</div>
<div class="flex items-center my-6 place-content-center">
    <a href="#join-us" class="btn btn-outline ">Let's build it together</a>
</div>
</div>
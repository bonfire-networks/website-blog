---
layout: layouts/base.njk
templateClass: tmpl-post
---


<div class="max-w-screen-sm px-6 mx-auto mt-0 mb-12 lg:px-0">
  <div class="py-8 pb-8 mx-auto">
    {# <a class="link mb-12  link-hover inline-flex items-center gap-2" href="/blog">
      <div class="rounded-full border w-8 h-8 border-base-content/30 border flex place-content-center items-center">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-blueGray-300" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
        </svg>
      </div>
        <span class="text-sm font-medium text-blueGray-300">Back to Newsroom</span>
    </a> #}
    <div class="">
       
     <div class="items-center justify-center mb-6 place-content-center flex gap-4">
      {%- for tag in tags | filterTagList -%}
      {%- set tagUrl %}/tags/{{ tag | slug }}/{% endset -%}
        <a href="{{ tagUrl | url }}">
        <span class="badge capitalize badge-lg badge-outline">
          {{ tag }}
        </span>
        </a>
      {%- endfor %}
      </div>
      <h1 style="line-height: 50px;" class="z-10 col-span-2 text-4xl font-bold text-center">{{title}}</h1>
     <div class="mt-4 mb-8 text-base text-center text-base-content/80">
        <span>Published {{ date | readableDate }}</span>
      </div>
     

      {% if header_html %}
        {{ header_html | safe }}
      {% else %}
        <img src="{{image}}" alt="post image" class="w-full max-h-[400px] mt-6 rounded-box object-fit object-cover" />
      {% endif %}
        
    </div>
  </div>

  <article class="max-w-screen-md mx-auto mt-12 prose prose-lg">
    {{ content | safe }}
  </article>


    
</div>

  <div class="mx-4 mt-8 lg:mx-auto">
    {% include "newsletter.njk" %}
    </div>

  
{# {%- set nextPost = collections.posts | getNextCollectionItem(page) %}
{%- set previousPost = collections.posts | getPreviousCollectionItem(page) %}
{%- if nextPost or previousPost %}
<hr>
<ul>
  {%- if nextPost %}<li>Next: <a href="{{ nextPost.url | url }}">{{ nextPost.data.title }}</a></li>{% endif %}
  {%- if previousPost %}<li>Previous: <a href="{{ previousPost.url | url }}">{{ previousPost.data.title }}</a></li>{% endif %}
</ul>
{%- endif %} #}

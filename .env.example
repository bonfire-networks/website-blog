# Stripe-hosted Payment Link for the Bonfire A3 poster.
# Local development uses the sandbox link automatically. Set this to test another link,
# and add the live value in Netlify so production uses real checkout instead of email ordering.
STRIPE_ROCCO_POSTER_URL=https://buy.stripe.com/your-live-payment-link
